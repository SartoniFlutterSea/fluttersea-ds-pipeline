# Segreti. Coprire OGNI variante: `*.env` da solo non prende `.env.local`,
# che e' proprio il nome usato per il token Figma.
.env
.env.*
*.env
*.env.*
!.env.example
*.pem
*.key
.npmrc

# Dipendenze e build
node_modules/
storybook-static/
dist/

# Derivati: si rigenerano
storybook/generated/
data/figma-pages.json

# IDE / OS
.idea/
.vscode/
.DS_Store

# Token risolti: valori reali dei brand, restano fuori dalla repo pubblica.
# Si rigenerano con: node tools/resolve-tokens.mjs <brand> <tema>
data/tokens/

# Inventario: si rigenera con tools/list-files.mjs
data/figma-inventory.json

data/figma-teams-inventory.json
data/figma-censimento.json
data/figma-descrizioni.json
data/mappa-componenti.json

# Grafo: derivato, si rigenera con tools/index_keys, graph_file, build_graph
data/grafo/
